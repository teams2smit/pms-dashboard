version: 0.2

phases:
  build:
    commands:
      - echo Entered build phase..
      - mvn clean package
    post_build:
      commands:
        - echo build completed successfully
artifacts:
  files:
    - target/pms-ui-service.jar
  discard-paths: yes